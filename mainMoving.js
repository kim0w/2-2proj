const img = document.querySelector(".img");

btn.addEventListener("click", clickHandler);

function clickHandler(){
  // ... 작업 로직 ...  //
  
  if(작업 성공){
    window.location.href = "new\global-master\index.html";
  }
}